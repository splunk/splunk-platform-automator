# Default values for os setting

os:
  # Do not set a default time zone, will default to the one from the ansible host
  #time_zone: "Europe/Zurich"
  #set_hostname: true
  disable_selinux: true
  packages: