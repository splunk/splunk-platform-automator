# Default values for aws hosts

aws:
  access_key_id: ENV['AWS_ACCESS_KEY_ID']
  secret_access_key: ENV['AWS_SECRET_ACCESS_KEY']
  region: 'eu-central-1'
  ssh_username: 'ec2-user'
  instance_type: 't2.micro'
