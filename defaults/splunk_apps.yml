# Default values for the splunk base_config apps

splunk_apps:
  # Pull rendered PS baseconfig apps into this lab folder (not apps_dir).
  splunk_save_baseconfig_apps_dir: saved_base_config_apps
  splunk_save_baseconfig_apps: false
  splunk_save_serverclass: false
  splunk_apps_dir: "../app_repo"