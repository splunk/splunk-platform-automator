---
# splunk_config.yml
plugin: splunk-platform-automator

######################################################################
# This is not a working example. Not for spa init --example.
#
# Operator and skill lookup: spa features list | search QUERY | show ID
# and spa features show ID --keys (schema types live in Pydantic).
# Catalog: examples/catalog/features.yml
#
# Remaining comments that are not a catalog record yet belong in
# spa features notes as they are folded. Do not copy live-looking
# passwords into YAML; use lookup('env', ...) like catalog snippets.
######################################################################
