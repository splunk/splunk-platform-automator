---

- name: Restart splunk
  tags:
    - splunk
    - splunk_common
    - splunk_restart
  ansible.builtin.service:
    name: "{{splunk_service_name}}"
    state: restarted
  become: yes
  become_user: root
