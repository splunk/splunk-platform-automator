---
# spa-run:
#   schema: 1
#   summary: Set up non-Splunk helper roles (pipeline step).
#   description: Configures extra roles such as syslog as part of deploy_site.
#   category: deployment
#   risk: mutating
#   requires_provisioned: true
#   examples:
#     - spa run setup_other_roles
# This playbook configures other roles

- name: Setup ldap server role
  hosts: role_ldap_server
  become: true
  become_user: root
  roles:
    - ldap_server

