# Minimal Ansible config for unit tests only.
# Used via ANSIBLE_CONFIG so the project's ansible.cfg (and config directory)
# are not loaded. Avoids inventory parsing and vault/config side effects.
[defaults]
# Path relative to this config file; must exist before tests run.
local_tmp = .ansible_tmp
