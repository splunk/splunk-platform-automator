# Extra vars for deploy_splunk_apps.yml: Splunkbase app, no credentials.
# Playbook should fail with Splunkbase credentials message when env vars are unset.
splunk_app_deployment:
  apps:
    - name: Splunk_TA_nix
      source: splunkbase
      app_id: "352"
      target_roles:
        - indexer
