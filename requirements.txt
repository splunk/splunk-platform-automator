ansible >= 3.4.0 # this is ansible-core 2.10.0
jmespath # required for json_query calls
lxml     # required for license file checks
pydantic >= 2.4.0 # required for config schema validation
boto3    # spa aws, suspend/resume, shell -l -v; unused until an AWS API call
